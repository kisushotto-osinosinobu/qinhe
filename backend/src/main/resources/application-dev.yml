spring:
  datasource:
    url: ${DB_URL:jdbc:mysql://localhost:3306/supermarket?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&useSSL=false}
    username: ${DB_USERNAME:supermarket}
    password: ${DB_PASSWORD:supermarket123}
  flyway:
    locations: classpath:db/migration,classpath:db/demo
app:
  jwt:
    secret: ${JWT_SECRET:dev-only-secret-change-before-production-123456}
